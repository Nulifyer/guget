{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "create-debug-dotnet",
            "type": "shell",
            "command": "cp -r ${workspaceFolder}/test-dotnet ${workspaceFolder}/debug-dotnet"
        },
        {
            "label": "remove-debug-dotnet",
            "type": "shell",
            "command": "rm -r ${workspaceFolder}/debug-dotnet"
        }
    ]
}